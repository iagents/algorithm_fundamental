#ifndef _BIT_WISE_OPERATORS_H__
#define _BIT_WISE_OPERATORS_H__

namespace algorithm_fundamental
{
  /// \brief Add two integer using the bitwise operators
  /// \param[in] x an operand
  /// \param[in] y another operand
  /// \return Sum of two integers
  int bitwise_add(int x, int y);
}

#endif
